# Issue 300 Stage 4 — implementation plan (workflow-drafted, adversarially attacked)

Produced 2026-08-29 by the stage4-implementation-plan workflow (2 codebase
mappers -> planner -> 2 attackers, ~700k tokens). STATUS: plan of record
WITH the amendments below — the builder works units + amendments together;
every DEFECT below is a mandatory fix to the corresponding unit before or
during its build. Volumes measured live the same day: organizations
12,586,144 (11,465,860 provisional), organization_names 4,364,021.

CONSUMER DECISION (Lennart, 2026-08-29, supersedes the deferred consumer
note in the design): the edge store's consumer is the ISSUE-311 per-case
AI review loop — edges and suspect classes queue individual agent reviews;
verdicts are recorded and applied only through the verified repair
machinery. Simple rules stay the deny-direction floor; they are not the
detector.

STAGE 4 IMPLEMENTATION PLAN — org_match_keys build + candidate-edge scans (E3/E4, no entity writes)
Issue 300 §2.3/§4.1/§6/§7/§8-Stage-4/§9; fixed decisions 1-8 honored as given. Volumes used throughout: organizations 12,586,144 (11,465,860 provisional / ~1.12M canonical), organization_names 4,364,021.

Session map: Session A = units 1-3; Session B = unit 4; Session C = unit 5 + first prod runs; Session D (optional, separable) = unit 6.

================================================================
UNIT 1 — N2/N3 key builders in ingest (Session A)
================================================================
Files: /home/user/tender-db/crates/ingest/src/project.rs (beside match_norm at :4126), /home/user/tender-db/crates/ingest/src/crosswalk.rs (legal_form_family at :529), crates/ingest/Cargo.toml.

New fns (pub, injected into store as plain fn pointers — the key-fn injection rule; store never depends on ingest):
- pub fn n2_key(name: &str) -> String
  N1 (str::to_lowercase, byte-identical to the 234 reuse key semantics) + NFKC + punctuation→space + whitespace collapse + quote fold (’‘‛`´ → '), dash fold (‐‑‒–—― → -, then - is punctuation→space). Diacritics deliberately preserved (decision 2; document in the doc comment with the M28 §6 rationale). NFKC needs the unicode-normalization crate — check crates/ingest/Cargo.toml first; if absent, add it (small, no_std-capable, pure Rust). Returns "" for names that normalize to nothing; callers skip empty keys.
- pub fn n3_key(name: &str) -> String
  n2_key output + legal-form CANONICALIZATION: each recognized legal-form token sequence replaced IN PLACE by a family token (e.g. "§llc-de", "§sro-cz", "§nordic-ab") — never stripped (stripping is reserved for E4/unit 6). Family table seeded from crosswalk::legal_form_family (:529) — extend that table rather than duplicating it: refactor legal_form_family so both the R2 veto and n3_key consume one token→family map (Oy/Ab/Oyj already folded to one Nordic family there; keep that). Must handle the punctuation-variant class from M28 §5: s.r.o. / s. r. o. / spol. s r.o. all → one token — note n2's punct→space folding runs FIRST, so the matcher operates on space-separated token runs ("s r o", "spol s r o"), which makes the multi-token match a token-sequence table, not a regex.
  Invariant to document + test: n3_key(name) == n2_key(name) exactly when no form token is recognized.

Tests (crates/ingest, unit tests beside the fns):
- NFKC fold (ﬁ ligature, fullwidth chars), quote/dash folds, punct collapse; diacritics preserved (Å ≠ A).
- n3 canonicalization: GmbH / G.m.b.H. / Gesellschaft mbH → same family token; s.r.o. variants → one key; Oy/Ab/Oyj → one Nordic token (the Linde/AGA rename shape); no-form name → n3 == n2.
- Empty/degenerate names ("", "---", "…") → "".
Acceptance gate: ingest tests green via ops/check.sh; the n2/n3 contract doc-commented (store tests will re-implement a local stand-in per the org_merge_health.rs convention, so the contract text is load-bearing).

================================================================
UNIT 2 — schema DDL + rebuild/reset wiring (Session A)
================================================================
Files: /home/user/tender-db/crates/store/src/canonical.rs (SCHEMA const, append after org_merge_log ~line 419, keeping the org cluster together), crates/store/src/lib.rs (MIGRATIONS ~549-585).

Exact DDL, appended to canonical::SCHEMA (both tables are NEW, so CREATE TABLE IF NOT EXISTS at open is free; no migration needed for the tables themselves):

-- issue 300 §2.3 (decision 1): REBUILDABLE scratch satellite. Name keys for the
-- Stage-4 E3/E4 candidate scans. Not an entity table: no change events, no FK
-- (org ids may dangle after merges — org_merge_log precedent), rebuilt wholesale
-- by build-org-match-keys whenever key semantics change. The (key_kind,key,org_id)
-- covering index is built POST-LOAD by the build job — NEVER add it to this
-- batch (the organizations_identity boot-hang lesson, see :339-357).
CREATE TABLE IF NOT EXISTS org_match_keys (
    org_id   INTEGER NOT NULL,
    key_kind TEXT    NOT NULL CHECK (key_kind IN ('n2','n3','n3s')),
    key      TEXT    NOT NULL
) STRICT;

-- issue 300 §6 (decision 3): candidate edges. E3 = exact name-key equality
-- (incl. cross-language via organization_names), E4 = weaker relations.
-- NEITHER EVER MERGES — this table is evidence accumulation only; consuming it
-- is a separate issue. state='approved' reserves a future human-gated path.
-- No FK on org ids (merged-away losers may be referenced; org_merge_log reasoning).
CREATE TABLE IF NOT EXISTS org_candidate_edges (
    org_a      INTEGER NOT NULL,           -- lower org id (CHECK enforces order)
    org_b      INTEGER NOT NULL,
    rule       TEXT    NOT NULL,           -- 'e3-name' | 'e3-xlang' | 'e4-stripped'
    tier       TEXT    NOT NULL,           -- 'E3' | 'E4'
    score      REAL    NOT NULL,           -- ordinal per rule, not probabilistic
    evidence   TEXT    NOT NULL,           -- JSON, hand-built (merge-log esc style)
    first_seen INTEGER NOT NULL,
    last_seen  INTEGER NOT NULL,
    state      TEXT    NOT NULL DEFAULT 'open',
    job_id     INTEGER,
    PRIMARY KEY (org_a, org_b, rule),
    CHECK (org_a < org_b)
) STRICT;

Notes locked in now:
- key_kind CHECK includes 'n3s' from day one even though unit 6 populates it later — CHECK cannot be altered in place, and while DROP+CREATE makes evolution possible, there is no reason to plan a second DDL churn.
- No index on org_candidate_edges beyond the PK: expected volume 10^5-10^6 rows; the exemplar probe does one bounded full scan per report; PK serves org_a-prefix probes. Revisit only if a consumer issue lands.
- org_match_keys PK: none (plain rowid table). Idempotence comes from (a) DROP+CREATE at rebuild start and (b) the watermark committing in the SAME transaction as each window (unit 3), so no window ever replays. Per-org key dedupe happens in Rust. This keeps the bulk load entirely index-free — the issue-60/62 thrash rule.

MIGRATIONS additions (lib.rs, ADD COLUMN only, per the :543-548 policy; both on projection_state, the id=0 control row — the rederive_eur_watermark precedent :624-628):
- ALTER TABLE projection_state ADD COLUMN org_match_keys_watermark INTEGER NOT NULL DEFAULT 0   -- build resume; 0 = no build in flight
- ALTER TABLE projection_state ADD COLUMN org_edge_total INTEGER NOT NULL DEFAULT 0             -- monotone-growth baseline (tripwire 6, unit 5)

Reset/rebuild wiring (decide-explicitly item, decided here):
- New store fn reset_org_match_keys(): DROP INDEX IF EXISTS org_match_keys_kk; then drop_and_recreate("org_match_keys") (canonical.rs:2856-2875 — with the index already dropped, sqlite_master replay recreates only the table, DDL stays single-sourced in SCHEMA). Called by the build job at wet start when watermark == 0.
- The bare org-layer rebuild path (strip_organization_indexes :2433-2471 / the path that drops organization_names at :2435): add DROP of BOTH org_match_keys and org_candidate_edges AND zero projection_state.org_edge_total + org_match_keys_watermark there. Rationale comment: a from-archive rebuild renumbers orgs, so keys and edges both dangle wholesale; the tripwire baseline reset here is the "legitimate wholesale reset" decision 7 must account for. Recreate via drop_and_recreate so the DDL text is not hand-copied (avoid the current_deadline duplicated-DDL trap noted at :2897-2903).

Tests: schema opens clean on a fresh DB and on an existing-fixture DB (IF NOT EXISTS path); STRICT + CHECK enforced (insert org_a > org_b fails); migration idempotent (duplicate-column swallow).
Acceptance gate: ops/check.sh green; Db::open on a copy of an existing test fixture shows no measurable open-time cost (no index in the batch).

================================================================
UNIT 3 — build-org-match-keys job (Session A, finish early Session B if needed)
================================================================
Files: crates/store/src/canonical.rs (impl Db, place beside the R2/R3 merge cluster ~5100-6100), crates/store/src/lib.rs (watermark accessors), /home/user/tender-db/crates/app/src/supervisor.rs (Spec variant, enqueue arm, run arm, STOPPABLE_KINDS, heavy_write_kind, consts).

Store fns:
- pub async fn org_match_keys_watermark(&self) -> turso::Result<i64> / set inside the window txn (NOT a separate write) — see below.
- pub async fn reset_org_match_keys(&self) -> turso::Result<()>   (unit 2)
- pub async fn build_org_match_keys_batch(
      &self,
      n2: fn(&str) -> String,
      n3: fn(&str) -> String,
      batch: i64,
      after: i64,
      dry_run: bool,
  ) -> turso::Result<(MatchKeyBuildWindow, i64)>   // (window report, new watermark); window.orgs == 0 ends the walk
  MatchKeyBuildWindow { orgs: i64, names_read: i64, n2_rows: i64, n3_rows: i64, empty_skipped: i64, rows_written: i64 }

Walk shape (the org_merge_health_batch precedent, canonical.rs:6415-6466 — window-bounds subquery, never bare rowid range; the rates.rs:390-397 collapse lesson):
1. Reader: SELECT id, name FROM (SELECT id, name FROM organizations WHERE id > ? ORDER BY id LIMIT ?) — batch = MATCH_KEY_ORG_WINDOW = 10_000 (new named const with rationale comment: 12.6M ids / 10k = ~1,260 windows; ~1.7 keys/org ⇒ ~17k rows/window, inside the NODE_WRITE_BATCH=20_000 sorted-bulk-load band :849; small 3-col rows).
2. Reader: satellite probe organization_names(org_id, lang, name) for the window's ids via IN_CHUNK = 512 probes (placeholders() helper; PK (org_id,lang) makes these indexed seeks; ≤20 probes/window).
3. Rust: per org, over head name + every satellite name: compute n2 (skip ""), and n3 only when n3 != n2 (lossless dedupe — a form token can never appear in a formless key, so no cross-kind group is lost; document this). Dedupe (kind,key) per org via HashSet.
4. dry_run: accumulate counts only, write nothing, return watermark.
5. Wet: one BEGIN IMMEDIATE; multi-row INSERTs in the flush_rows shape (canonical.rs:8749; MAX_BATCH_BIND 900 / 3 cols = 300 rows per statement); then UPDATE projection_state SET org_match_keys_watermark = ? IN THE SAME TXN; COMMIT; on any Err: ROLLBACK (poisoned-txn guard, the :5567-5589 shape). Watermark-in-txn ⇒ exactly-once per window, no PK needed, crash redoes nothing.
- pub async fn finish_org_match_keys(&self) -> turso::Result<i64>  // returns final row count
  CREATE INDEX org_match_keys_kk ON org_match_keys(key_kind, key, org_id)  — covering, so unit 4's walk is index-only; built ONCE post-load. Memory: worst case 34M rows × 48 B (INDEX_BUILD_BYTES_PER_ROW :36-59) = 1.63 GB, well under AUTO_INDEX_MEMORY_BUDGET 12 GB; sequential with nothing (supervisor Reindex constraint respected because it runs inside this one job). Then TRUNCATE checkpoint immediately (the build_plan_groups "reclaim the WAL tail" precedent :3823-3835), then ANALYZE org_match_keys non-fatal (:3842-3846), then reset watermark to 0 (separate small txn).

Supervisor wiring:
- Spec::BuildOrgMatchKeys { dry_run: bool } (unit-variant-plus-flag; serde externally-tagged, forward-safe).
- Enqueue arm kind "build-org-match-keys", dry_run = req.dry_run.unwrap_or(true) (§7 blanket rule; a forgotten flag must mean the harmless thing). Params string: "dry|wet batch=10000" (audit line convention).
- Run arm: if wet and watermark == 0 → reset_org_match_keys() (DROP+CREATE, index dropped). If wet and watermark > 0 → log "resuming past id N", do NOT reset (crash-resume). Loop: cancelled(job.id) check at top of every batch (stopped=true; a stopped WET build leaves watermark standing so the next run resumes — the honest state; a stopped build records NO report); call batch fn; set_phase("walking", Some(orgs_done), None, "id ..= {watermark}, window {ms}ms") — done-without-total per issue 228; per-window elapsed logged (the DQ_WINDOW journal line discipline); self.db.checkpoint(Truncate) between batches, error eprintln'd only (issue 42). On walk end, wet: set_phase("indexing"), finish_org_match_keys(), set_phase("analyzing" is inside finish; fine as one phase).
- Reports: dry → put_report("org-match-keys-plan", {orgs, names, projected_rows, n2, n3, empty_skipped, windows, elapsed}); wet → put_report("org-match-keys-build", {rows, n2, n3, index_built: true, built_at, elapsed}). built_at doubles as the build epoch the scan report cites. Dry-run summary line SCREAMS "DRY RUN — STORED NOTHING" (issue 272). Wet does NOT require a stored plan — documented deviation from the T4 ladder: nothing here is irreversible (scratch DROP+CREATE); T4 stays reserved for the scan whose volume bounds are a §8 gate.
- STOPPABLE_KINDS (:1176): add "build-org-match-keys" (the loop genuinely reads the flag) + update the contract test's exact array literal and per-kind comment (:5032-5086 style). heavy_write_kind (:4696): add it (17-34M row writes — issue-281 rule: every batched writer belongs on the list) + update heavy_write_in_progress_tracks_the_running_job_kind (:5569).
- NO change events, NO publish_cursor anywhere in this path (decision 6).
- No auto-enqueue at boot (would break recovers_the_queue_across_a_restart :5767).

Sizing/estimates to record in the unit's journal entry:
- Expected rows: 12.6M head N2 + ~4.4M satellite N2 (minus dupes) + N3-only rows at the measured 27.3% suffix coverage (~4.6M) ≈ 19-22M; the 17-34M design band holds, upper bound if dedupe underdelivers.
- Duration: read+compute ~15-30 min (org-merge-health walks the corpus in ~2 min read-only; this walk adds the satellite probes and key computation), wet fill ~45-90 min at the 307 backfill's measured class (38.7M REPLACE ops in one job), index ~2-5 min (organization_mentions' 41M-row build "measured fine"). Whole wet job < 2 h; watermark makes a crash lose at most nothing (exactly-once windows).
- Disk: table ~55 B/row ≈ 1.1-1.9 GB; covering index similar ⇒ steady ~2.5-4 GB on the 1.7 TB box. WAL bounded by per-window TRUNCATE + heavy_write registration (the coverage-refresher stand-down, issue 53).

Tests:
- Store (new crates/store/tests/org_match_keys_build.rs, org_merge_health.rs shape: raw conn seed, FK off, local n2/n3 stand-ins documenting the ingest contract): batch=1 walk — watermark advances and terminates (rounds < 10); head + satellite keys both land; per-org dedupe (same key via head and satellite = one row); n3 stored only when != n2; empty keys skipped and counted; dry run writes zero rows; wet rebuild after a semantics change (different injected fn) yields no stale rows (DROP+CREATE proven); index exists after finish (sqlite_master probe); resume: kill after window 1 (simulate by calling batch fns manually), rerun without reset, no duplicate rows.
- No-entity-writes (the (c) strategy, full shape in unit 4 — apply here too): SELECT COUNT(*) FROM changes == 0 (org_merge_change_events.rs:146 verbatim); COUNT + content hash over organizations/organization_mentions/organization_names before vs after; latest_cursor unchanged.
- Supervisor: enqueue defaults to dry ("the_mark_job_defaults_to_a_dry_run" :5919 shape); STOPPABLE contract test updated; heavy test updated; queue recovery test still green.
Acceptance gate: ops/check.sh green; fixture build produces exactly the hand-computed key rows; then Session-A close = deploy-ready code, no prod run yet.

================================================================
UNIT 4 — scan-org-match-keys job: E3 edges, stoplist, bounds (Session B)
================================================================
One job, one index walk. Design §7 names two scans (B-NAME/B-XLANG), but the satellite already unified the key space — satellite rows are IN org_match_keys, so cross-language equality IS key equality on this index. The split survives as two RULE labels decided per pair from evidence (below). Record this collapse in the job's doc comment citing §4.1.

Files: canonical.rs (impl Db beside the merge cluster), supervisor.rs.

Store fn (whole-corpus single fn with internal windows, the R2-merge shape — group boundaries and report accumulation live naturally inside; NOT a batch-fn-per-call):
- pub struct OrgEdgeScanArgs<'a> { pub n2: fn(&str)->String, pub n3: fn(&str)->String, pub stoplist_cap: i64, pub max_edges: Option<i64>, pub dry_run: bool, pub job_id: i64, pub stop: &'a (dyn Fn() -> bool + Sync) }
- pub async fn scan_org_match_keys(&self, args: OrgEdgeScanArgs<'_>, now: i64) -> turso::Result<OrgEdgeScanReport>
  OrgEdgeScanReport { keys_walked, groups_ge2, groups_emitting, provisional_only_groups, stoplist_skipped_n2, stoplist_skipped_n3, stoplist_top: Vec<(String,i64)>, pairs_considered, edges_written, edges_new, edges_refreshed, per_rule: [(rule, count)], max_group, exemplar: ExemplarProbe, total_edges_after, capped: bool, stopped: bool }

Walk shape:
1. Per kind in ['n2','n3'] (order matters — n2 first, see dedupe below): keyset windows on the covering index via the reader pool: SELECT key, org_id FROM org_match_keys WHERE key_kind = ? AND key > ? ORDER BY key, org_id LIMIT ? — SCAN_KEY_WINDOW = 200_000 (new const; rationale: ~40 B/row ⇒ ~8 MB per page in RAM, well inside the read-side WINDOW=500_000 precedent :2274; equality+range on a named index per §7 — never a bare rowid range). Drop the trailing possibly-partial key group from each page and set the watermark to the last COMPLETE key (lookahead trim), so groups never straddle windows.
2. Group per key. size < 2 → skip. size > STOPLIST_CAP = 20 (new const; decision 5, the "Gymnázium"/"Centre hospitalier" class) → increment per-kind stoplist counter, feed a BinaryHeap<Reverse<(count,key)>> top-20 sample (R2Census denial-counter style: "counted so the report shows the walls working"), emit NOTHING for the key.
3. Classify members: bounded IN probe (IN_CHUNK 512 — trivially one chunk at ≤20) into organizations for id + the canonical/provisional predicate (identifier-bearing = canonical — the exact predicate the R2 preload uses; reuse it, don't re-derive). Emission rule (decision 4, §4.1): pairs = canonical×canonical plus provisional×canonical; provisional×provisional NEVER (all-provisional groups counted as provisional_only_groups, emit nothing — the 234 line).
4. Rule label per pair: re-read both orgs' names via Db::org_all_names (canonical.rs:7217) and recompute keys with the injected fns; if both sides reach the group key through their HEAD name → rule 'e3-name' (score 1.0); if at least one side reaches it only through a satellite row and the reaching rows' langs differ → 'e3-xlang' (score 0.9); satellite-same-lang fallback → 'e3-name'. tier 'E3' both. Cheap: groups ≤20, org_all_names is head + ≤ a few dozen satellite rows.
5. Per-run in-RAM emitted-set HashSet<(a,b,rule)>: an n3 group re-finds most n2 groups (both orgs "x gmbh" group under both kinds) — the set keeps edges_written honest and avoids double upserts (≤10^6 × 24 B, bounded).
6. Edge upsert preserving first_seen/state: for each write batch, read existing (org_a,org_b,rule)→(first_seen,state) via IN probes on the PK, then INSERT OR REPLACE carrying preserved first_seen/state and last_seen = now, evidence refreshed. (Read-then-REPLACE, not ON CONFLICT upsert — only constructs the codebase already exercises on turso.) Writer connection; EDGE_WRITE_BATCH = 10_000 rows per BEGIN IMMEDIATE txn (BACKFILL_BATCH class: small rows + indexed PK maintenance at 10^5-10^6 total is fine); ROLLBACK-on-err including failed COMMIT (poisoned-txn guard); TRUNCATE checkpoint after each write txn.
7. Evidence JSON, hand-built with the esc closure precedent (:5522-5546, control chars stripped): {"kind":"n2","key":"…","group":N,"a":{"src":"head"|"lang:nl","name":"…"},"b":{…}} — the corroboration-provenance discipline (§4.2 T7): one contaminated satellite row must be findable from its edges.
8. dry_run: steps 1-5 run fully, step 6 skipped; report is the would-emit census. Cancel (stop() between windows): wet keeps edges written so far (idempotent refresh — harmless), but records NO report and shows an honest stop message (the R2/R3 classify-phase-cancel hardening); dry-cancel records nothing (the issue-230 zero-lie bar).
9. max_edges: clean stop when hit, capped=true, no report→ wait — a capped wet run DOES record its report but with capped=true and does NOT update the monotone baseline (unit 5); operator guard in the MergeProvisionalOrgs max_groups mold.

Preconditions (refuse-with-remedy, the has_index precheck precedent :3494-3508):
- has_index("org_match_keys_kk") else refuse: "run build-org-match-keys (wet) first".
- projection_state.org_match_keys_watermark == 0 else refuse: "keys build in flight; let it finish or rerun it".
- latest_report("org-match-keys-build") present; its built_at is echoed into the scan report (lineage).

Supervisor wiring:
- Spec::ScanOrgMatchKeys { dry_run: bool, max_edges: Option<i64> }; kind "scan-org-match-keys"; dry_run = req.dry_run.unwrap_or(true). Params string names dry/wet, cap, stoplist_cap, and the keys build epoch (audit line).
- T4 ladder (bounds, decision 8): a WET run REQUIRES latest_report("org-edge-scan-plan") (recorded only by a completed dry run against the SAME keys build epoch) and aborts before writing when the live would-emit count diverges from the plan beyond max(2%, 500) — implemented as R2 does it: dry-census phase first, one whole-plan pre-write parity check. The plan report carries bounds_ok computed against EDGE_VOLUME_CEILING = 3_000_000 (named const; rationale comment: order-of-magnitude tripwire only — the a-priori pools are the AT 46.9%/CZ 49.2%/PT 25.2% name-dup rates over ~1.12M canonical rows and the 70,598 multi-lang satellite orgs bounding xlang, both pointing at 10^5-10^6 pairs; the REAL bound is the reviewed dry-run number). Wet refuses when bounds_ok=false — no override flag; an out-of-bounds census means the semantics are wrong, re-plan.
- Phases: "censusing" (dry walk; done = keys_walked, detail = "kind=n2 key>… window {ms}ms"), "emitting" (wet), per-window elapsed logged.
- Reports: dry → put_report("org-edge-scan-plan", census + bounds_ok + keys_build_epoch); wet → put_report("org-edge-scan", full report + monotone fields from unit 5). Both served by GET /admin/reports/{kind} with zero admin code. Dry summary: "DRY RUN — STORED NOTHING".
- STOPPABLE_KINDS + contract-test literal; heavy_write_kind + its test (wet writes + the census read walk both pin WAL; issue-281 under-inclusion rule).
- Reads via self.reader(), writes via self.conn() (convention (b)). NO append_change, NO publish_cursor (decision 6).

Tests (new crates/store/tests/org_candidate_edges.rs + supervisor tests):
- Two canonical orgs sharing an N2 key → one e3-name edge, org_a < org_b, evidence names both sources; provisional+canonical → edge; provisional-only group → nothing + provisional_only_groups=1; 21-org group → zero edges + stoplist counter + key in stoplist_top.
- xlang: org A head "PostAuto AG" (de) + satellite fr "CarPostal SA"; org B head "CarPostal SA" → e3-xlang with lang provenance in evidence.
- Contamination fixture (the 23294544 shape): org X carrying a foreign satellite name equal to org Y's head name → edge X-Y and NOTHING else (no other table moves).
- Refresh semantics: second run preserves first_seen and state (seed state='approved' by hand, assert survives), bumps last_seen; edges_new=0, edges_refreshed>0.
- n2+n3 double-discovery: same pair via both kinds → ONE row, ONE write (the emitted-set).
- Dry run: zero rows in org_candidate_edges, plan recorded; wet without plan → refused; parity divergence (mutate corpus between dry and wet beyond tolerance) → abort, nothing written; cancel mid-walk → no report recorded.
- THE (c) NO-ENTITY-WRITES ASSERTS, both build and scan tests, verbatim shape: assert_eq!(scalar(&conn, "SELECT COUNT(*) FROM changes").await, 0, "no change rows at all") (org_merge_change_events.rs:146); COUNT(*) parity + content hash over organizations, organization_mentions, organization_names before vs after; latest_cursor()/current_cursor() unchanged; org_merge_log COUNT unchanged. (Prod-side equivalent is defined in unit 5 — on a live box the cursor moves with ingest, so the prod evidence is org_merge_log parity + journal + the report's structural zero-merge shape, not cursor stillness.)
- Supervisor: scan defaults to dry; index-missing refusal message; STOPPABLE/heavy/recovery tests updated.
Acceptance gate: ops/check.sh green; every fixture edge hand-verifiable from its evidence JSON alone.

================================================================
UNIT 5 — tripwire 6, exemplar probe, cadence, prod rollout (Session C)
================================================================
Files: supervisor.rs (scan run arm additions, scheduler tick), canonical.rs (baseline accessors), runbook = the issue file's Stage-4 log section.

(e) Edge-store monotone-growth alarm (tripwire 6):
- Durable baseline = projection_state.org_edge_total (unit 2 migration; the roll_reveal_wrap accumulate-and-roll precedent :4735-4761 argues for a durable row over report-body diffing — reports get overwritten).
- At the END of every completed, uncapped WET scan: total_edges_after = COUNT(*) FROM org_candidate_edges; compare to baseline: total < baseline → alarm=SHRUNK (RED — edges are append-refresh-only in v1; there is no legitimate shrink except the bare org rebuild, which zeroes the baseline in unit 2's wiring, re-anchoring exactly as decision 7 requires); total - baseline > EDGE_VOLUME_CEILING-scale growth or > 2× the recorded plan delta → alarm=SPIKE (RED — a new generic-name family or a broken key fn). Alarm fields land in the "org-edge-scan" report + an eprintln; then baseline := total. Capped/stopped runs never touch the baseline.
- Cadence: after stage acceptance (below), add the wet scan to the Sunday pre-dawn REPORT_TICK chain (spawn_report_scheduler :4502-4553) behind already_pending("scan-org-match-keys") (:1430 — the issue-282 double-enqueue guard). The weekly refresh IS the tripwire's clock: last_seen advances, monotone check runs, report overwrites. The build job is NOT scheduled — it reruns only on key-semantics changes (rebuild-wholesale contract) or after a bare rebuild; note in the run arm that a scan refusing on a missing index is the operator's signal to run the build.
- Follow-up filed, not built: incremental key maintenance for NEW orgs (the resolver-side split the satellite backfill has for names). Until it lands, keys staleness is bounded by the weekly... no — keys are built once; new orgs are invisible to scans until the next build. Accepted for v1 (edges are advisory; a monthly-or-on-demand rebuild is cheap at <2 h) — file it as the Stage-4 follow-up issue with the incremental-vs-rebuild decision explicitly open.

(d) org-23294544 exemplar check, wired into every scan run (dry and wet):
- Resolve 23294544 through org_merge_log loser→keep chase (bounded walk; Stages 1-3 may have merged it) to the standing id.
- Probe: one bounded scan of org_candidate_edges for edges touching the resolved id; record {resolved_id, edges: n, rules, peers} in the report's exemplar block.
- Gate reads: ≥1 edge (expected e3-xlang — the contaminated Dutch-MoD satellite row keys onto the real MoD org), every touching edge state='open', zero org_merge_log rows for it in the scan window. "…and nothing else" = the report's zero merge/change counters + org_merge_log COUNT parity across the run + clean journal.

Prod rollout sequence (each step gated on the previous, all operator-fired via POST /admin/jobs; box serves reads throughout — heavy_write registration is what buys the TRUNCATE instants):
1. Deploy. build-org-match-keys DRY → read "org-match-keys-plan": projected rows in [17M, 34M] (expected ~19-22M), window timings sane. 
2. build-org-match-keys WET (~<2 h, resumable). Read "org-match-keys-build": rows match plan within ingest drift; index built; wal_bytes()/diag log clean (use Db::log_diag for the build heartbeat — duration-based, the PREV_EDGE_HEARTBEAT 15 s discipline).
3. scan-org-match-keys DRY → read "org-edge-scan-plan": bounds_ok true; groups/stoplist/exemplar reviewed BY HAND (the dry census is the pre-estimate decision 8 demands — record the reviewed numbers in the issue file before any wet run).
4. scan-org-match-keys WET capped (max_edges 200_000) → tripwires green, exemplar block green, spot-check 20 edges by hand against evidence JSON.
5. Uncapped WET under parity → baseline anchored, report reviewed.
6. Flip the Sunday tick on. Refold canary (tripwire 7) after the campaign: sampled refold rewrites zero mention bindings — Stage 4 wrote no bindings, so any movement is a foreign bug.
STAGE 4 ACCEPTANCE (mirrors §8): no-entity-writes tests green in CI AND prod org_merge_log/journal evidence clean; edge volumes within the step-3 reviewed bounds; stoplist counter nonzero with sensible top keys (the cap visibly working); exemplar surfaced as edge(s) and nothing else; monotone baseline anchored.

================================================================
UNIT 6 — E4 slice: stripped keys + e4-stripped edges (Session D, OPTIONAL — separable, ship Stage 4 without it if time runs out; §8 lists E4 in-stage, so it is planned, not dropped)
================================================================
- ingest: pub fn n3s_key(name: &str) -> String = n2 with recognized legal-form token sequences REMOVED (stripping, reserved for E4 per decision 2), collapsed; returns "" when the stem is empty or < 4 chars (a bare "gmbh" must not become a key). Tests: "X GmbH" vs "X AG" → same n3s; stem-floor cases.
- build job: populate key_kind 'n3s' (CHECK already admits it; stored only when != n2 and nonempty) — a semantics change, so the rollout is: deploy, rerun build wet (DROP+CREATE handles it), rerun scan dry→wet. Row delta ≈ the same 27.3% form-coverage class as n3, ~+4-5M rows — still inside every budget.
- scan: 'n3s' groups → rule 'e4-stripped', tier 'E4', score 0.5; same stoplist cap, same emission rule, evidence carries both sides' full (unstripped) reaching names — this is the Organschaft signature generator ("X GmbH"/"X AG" sharing a VAT is denial-7's shape; these edges are its future evidence feed).
- Gates: Organschaft fixture yields e4-stripped and no E3; e4 volume within its own dry-run census; all unit-4/5 tripwires stay green after the rebuild (the monotone baseline survives a keys rebuild because edges are never dropped by it — only bare org rebuilds reset).

================================================================
(f) EXPLICITLY OUT OF THIS STAGE
================================================================
- Any entity write, merge, or mention re-route from name evidence — E3/E4 never merge (decision 3); no append_change/publish_cursor anywhere in Stage-4 code paths (decision 6).
- Acronym↔expansion E4 (CNFPT class) — no generator mechanism in v1; needs its own design.
- Diacritic folding (decision 2, v1 exclusion).
- Edge consumption: no review UI, no serving surface, no state-machine beyond the reserved 'approved' literal, no stale-sweep (edges accumulate; the sweep §6 mentions is a follow-up and must be co-designed with tripwire 6, which currently assumes append-only).
- Stoplist feedback into R3 corroboration ("stoplisted keys disqualified as R3 corroboration unless hard-checksum", §4.1) — R3's shipped slices already ran; wiring the stoplist into FUTURE R3 slices (the multi-anchor SI design, issue-310 prevention) is filed as a follow-up issue, not built here.
- Incremental key maintenance on new-org mint (resolver-side) — follow-up issue; v1 staleness is bounded by on-demand rebuilds.
- N3 keys feeding richer R3 corroboration, DE court-scope demotion, r3-unanchored promotion — Stage 6 by design.
- Any schema change to organizations; name_norm, the E0 key, and the 234 reuse key untouched (§10).

Key existing-code anchors for the builder: canonical::SCHEMA :97-774 (+organization_names :398-404, org_merge_log :411-419), drop_and_recreate :2856-2875, deferred-index machinery :2493-2753 (pattern reference only — Stage 4 builds its own index in-job), flush_rows :8749 / MAX_BATCH_BIND :8745 / IN_CHUNK :8836, checkpoint.rs (Truncate + wal_bytes + log_diag), org_merge_health_batch :6415-6466 (walk), org_all_names :7217, put_report/latest_report :8301-8316, R2 merge txn discipline :5471-5591, supervisor Spec :199-435, enqueue arms :661-1055, STOPPABLE_KINDS :1176 + contract test :5032, heavy_write_kind :4696 + test :5569, already_pending :1430, scheduler tick :4502-4553, rederive watermark rates.rs:402-421, ingest::project::match_norm :4126 / normalize_lang :4073, ingest::crosswalk::legal_form_family :529, test shapes crates/store/tests/{org_merge_health.rs, org_name_backfill.rs, org_merge_change_events.rs:146}.

## MANDATORY AMENDMENTS — fidelity attacker findings

- DEFECT (Unit 1, HIGH — the plan forks N2). ingest::project::match_norm (/home/user/tender-db/crates/ingest/src/project.rs:4116-4141) already IS the shipped issue-300 N2 key: doc-commented "The matcher's N2 name key (issue 300 §2.3)", and live-injected as the N2 in the resolver (project.rs:1246, 1952), the org-merge-health census that anchors tripwire 2 (supervisor.rs:2389/2520/2712), the R3 corroboration key (canonical.rs:1448 "project::match_norm — the N2 corroboration key", :1642), and match_org_null_country_r3 (supervisor.rs:3000). It deliberately EXCLUDES NFKC ("deferred until something measured demands it") and subsumes quote/dash/punct folding in one non-alphanumeric→space rule. The plan's Unit 1 specifies a NEW n2_key = N1+NFKC+quote/dash folds without ever mentioning match_norm's existing role — creating two divergent 'N2' keyspaces (Stage-4 edges/stoplist vs R3 corroboration/census/prevention), the exact drift the codebase's own injection comments forbid (crosswalk.rs:484-487: "the two MUST share one crosswalk, or prevention and repair drift apart"; §4.1's stoplist-disqualifies-R3-corroboration linkage only makes sense on ONE keyspace). Required: decide explicitly — (a) upgrade match_norm in place to add NFKC (one N2 everywhere; reverses a recorded in-code decision, shifts the tripwire-2 census keys and R3 corroboration semantics, needs a baseline note), or (b) reuse match_norm verbatim as n2_key and record the NFKC deviation from fixed decision 2 on the board. As written the plan silently does neither.

- DEFECT (Unit 1, LOW, folds into the above): the plan's quote-fold (’‘‛`´→') and dash-fold (‐‑–—→-) steps are no-ops — the folded characters are themselves punctuation and get folded to space next (match_norm's doc comment states the alnum filter subsumes these classes; test project.rs:4657 already pins typographic-quote folding). Unit 1's test list would be testing dead steps. Also the 'N1 = str::to_lowercase byte-identical to the 234 reuse key' claim is not exactly what match_norm does (per-char c.to_lowercase(), which differs from str::to_lowercase on Greek final sigma) — moot if match_norm is reused, another drift trap if a second fn is written from the plan text.

- DEFECT (Unit 4, MEDIUM — cited fn cannot serve its step). Db::org_all_names (/home/user/tender-db/crates/store/src/canonical.rs:7217-7234) returns Vec<String> — names only, no head-vs-satellite provenance and no lang (both its queries SELECT name only; no other store reader returns satellite langs, checked :1288/:4237/:7228). Unit 4 step 4 (rule labeling: head-reach vs satellite-reach, 'reaching rows' langs differ') and step 7 (evidence JSON "src":"head"|"lang:nl") both require (source, lang, name) per reaching row. A new provenance-carrying read is needed (or org_all_names extended, which touches its R3 call sites). Additionally the labeling rule is under-specified for the head-vs-satellite case — the head name has no lang to 'differ' with, and the design's flagship B-XLANG exemplar (PostAuto AG head / CarPostal SA fr satellite vs CarPostal SA head) is exactly that case; the plan must state that head-vs-foreign-satellite reaches count as e3-xlang. (Cosmetic: organization_names.lang is ISO 639-2/T uppercase, 'NLD' not 'nl' — canonical.rs:400.)

- DEFECT (Unit 4, MEDIUM — scan walk can fail to terminate). The lookahead trim ("drop the trailing possibly-partial key group, watermark = last COMPLETE key") with a key-only watermark does not terminate when a single key's rows fill the whole SCAN_KEY_WINDOW=200,000 page: every row on the page is the same key, the whole page is trimmed as 'possibly partial', the watermark cannot advance, and the next `key > ?` query re-reads the identical page — an infinite loop on the prod box (stoppable only via cancel). Needs a same-key-fills-page guard: such a key is >STOPLIST_CAP by construction, so count it into the stoplist counters/top-sample and advance the watermark past it. Low probability at 200k orgs per key, but the guard is two lines and the failure mode is a spinning heavy job on the serving box.

- DEFECT (Unit 5 × Unit 4, MEDIUM — the weekly cadence deadlocks against the T4 gate). Unit 4 makes a WET scan REQUIRE latest_report("org-edge-scan-plan") "recorded only by a completed dry run against the SAME keys build epoch" and parity-abort beyond max(2%,500); Unit 5 then schedules only the WET scan on the Sunday tick and never re-records the plan after a wet run (unlike R2, which re-records the RESIDUAL plan precisely so continuations pass parity — supervisor.rs:2871-2916, the verifier catch the plan itself cites elsewhere). With static keys and a moving corpus (cadenced R2/R3 merges deleting orgs, classification drift), the live would-emit census diverges from the one aging dry plan until every Sunday wet run parity-aborts — silencing exactly the tripwire-6 clock (monotone check + last_seen refresh) the cadence exists to run; a keys rebuild makes it immediate via the epoch mismatch. Fix explicitly: chain dry→wet on the tick, or have completed uncapped wet runs re-record org-edge-scan-plan (the R2 shape), and say which in the plan.

- DEFECT (Unit 6, MEDIUM — e4-stripped misses the bare-stem class). Storing n3s only when n3s != n2, with groups formed strictly per (key_kind, key), means an org named bare "X" (no legal form) stores no n3s row, so "X" vs "X GmbH" — legal-form-stripped equality under §1's E4 definition, and the commonest E4 shape (the same name published with and without its form) — is found by NO kind: n2 keys differ ("x" vs "x gmbh"), n3 keys differ, and "X" is absent from the ('n3s',"x") group. The Unit-3 'lossless dedupe' argument (a form token can never appear in a formless key) is valid for n3 CANONICALIZATION but was implicitly and wrongly extended to n3s STRIPPING, where the formless name's n2 DOES equal the formed name's n3s. Fix: at scan time union ('n3s',k) group members with ('n2',k) members (cheap indexed probe), or store n3s rows for stem-only names when any formed sibling exists — pick one and record it; the Organschaft pair ("X GmbH"/"X AG") works either way.

- DEFECT (Unit 3, LOW — resume crash-window). finish_org_match_keys specifies bare `CREATE INDEX org_match_keys_kk ...`. A crash between the index build and the watermark reset (or the checkpoint/ANALYZE between them) leaves watermark > 0 with the index present; the next wet run takes the resume path (no reset), the walk immediately ends, finish re-runs, and CREATE INDEX hard-errors 'already exists' — and re-running fails identically, so the operator is wedged until a manual reset. One-word fix: CREATE INDEX IF NOT EXISTS (the codebase's own idiom, e.g. plan_notice_fold at canonical.rs:3824-3830), or DROP INDEX IF EXISTS first inside finish.

- DEFECT (Unit 5, LOW — the dry-run exemplar block is vacuous before the first wet run). The exemplar probe is 'wired into every scan run (dry and wet)' but reads org_candidate_edges — which is empty at rollout step 3 (the hand-review of the dry census, BEFORE any wet run), so the reviewed exemplar block necessarily shows 0 edges and the '≥1 edge' gate can only ever be read post-wet. Since dry runs execute steps 1-5 fully, the dry-run probe should consult the in-RAM would-emit set (plus the edge table), so step 3's hand review can actually confirm the 23294544 pair will surface. Related unchecked premise: the emission rule (provisional×provisional never emits) means the exemplar gate silently depends on at least one side of the resolved 23294544 pair being identifier-bearing — worth one bounded prod SELECT before the rollout rather than discovering it at step 4.

- DEFERRED OBLIGATION with a live interaction (Unit (f) scope cut, LOW): §4.1 stoplisted keys are also 'disqualified as R3 corroboration unless the identifier hard-checksum-passes'. The plan files this as a follow-up (defensible — R3's slices shipped), but the design doc's Stage-3 close (issue file line 533) holds a PENDING decision to fold rule=r3 into the SAME Sunday cadence Stage 4 turns on. If r3 joins the cadence before the follow-up lands, generic-name corroboration (the Gymnázium/Centre-hospitalier class) stays live in a real merge path while the stoplist that names those keys sits one report over. The plan should gate the r3-cadence decision on the stoplist wiring (or note the ordering) — one sentence in Unit 5.

- RATIONALE ERROR, no behavior change (Unit 4 step 6, LOW): 'Read-then-REPLACE, not ON CONFLICT upsert — only constructs the codebase already exercises on turso' is factually wrong — put_report (canonical.rs:8301-8310) is an ON CONFLICT(kind) DO UPDATE upsert running on turso in prod. `INSERT ... ON CONFLICT(org_a,org_b,rule) DO UPDATE SET last_seen=excluded.last_seen, evidence=excluded.evidence, job_id=excluded.job_id` would preserve first_seen/state in one statement with no extra PK read pass. Read-then-REPLACE still works; the justification for choosing the heavier construct is false.

- MINOR/UNSPECIFIED (sweep, no redesign implied): (a) Unit 4 step 3 doesn't say what happens to group members whose org id no longer resolves (org_match_keys rows dangle after cadenced merges by design) — the natural implementation (members = probe results) is right, but state it so a test pins it; (b) cited line numbers have drifted: has_index is at canonical.rs:7239 (plan says :3494-3508 — that precedent text lives elsewhere), the heavy-write test is ~:5569 area as cited but the esc closure is :5525-5534; harmless, but Session-A implementers should grep, not trust offsets; (c) STRICT tables with an IN-list column CHECK and a cross-column CHECK (org_a < org_b) are NEW constructs on this turso — existing schema CHECKs are only `id = 0` (canonical.rs:582/598/619); the plan's own enforcement tests cover it, just run them before relying on the constraint as the sole ordering guard; (d) unicode-normalization is absent from the workspace as the plan suspects (checked all Cargo.tomls) — needs a workspace-level entry too if finding 1 resolves toward NFKC; (e) design §7 names two scan jobs (scan-org-name-blocks / scan-org-xlang-blocks); the plan's single-job collapse with per-pair rule labels is sound and self-documented — record it in the issue file as a deviation from §7's naming, not just in the code doc comment; (f) the wet BUILD skipping §7's blanket dry-plan parity requirement is a real deviation from the design's 'every wet batch compares to the recorded dry-run plan' rule — the plan's rationale (rebuildable scratch, no entity writes, watermark exactly-once) is good; it is flagged in-plan, keep it flagged on the board too.

- VERIFIED SOLID (for the record, so the other lenses need not re-check): schema anchors (organization_names PK(org_id,lang) :398-404, org_merge_log :411-419, organizations_identity boot-hang comment :339-357), drop_and_recreate replay semantics :2856-2875 (drop-index-first is exactly right), strip_organization_indexes :2433-2471 as the bare-rebuild hook point, projection_state id=0 + add_column/rederive precedent (lib.rs:624-628, rates.rs:402-421), watermark-in-same-txn design (stronger than the rederive write-after precedent, correctly reasoned), MATCH_KEY_ORG_WINDOW/SCAN_KEY_WINDOW sizing vs NODE_WRITE_BATCH=20_000 (:849) and WINDOW=500_000 (:2274), index memory math vs INDEX_BUILD_BYTES_PER_ROW=48/AUTO_INDEX_MEMORY_BUDGET=12GB (:36-76), finish-job checkpoint+ANALYZE precedent :3823-3846, R2 txn/poisoned-COMMIT/esc discipline :5471-5591, R2 preload predicate `identifier IS NOT NULL` :5183 (matches the plan's canonical test), fn-pointer injection shape (R2MergeArgs :1371-1390, mention_resolver 5-slot injection, org_merge_health_batch :6415), supervisor surface (Spec/enqueue dry-default, STOPPABLE contract literal :5069-5085, heavy_write_kind :4697, already_pending :1430, REPORT_TICK scheduler :4502-4553, set_phase :1490, cancelled :1240, checkpoint-with-eprintln idiom :2097), generic GET /admin/reports/{kind} (admin.rs:39), the no-entity-writes assert shape (org_merge_change_events.rs:146 verbatim), the n2/n3 lossless-dedupe invariant (correct FOR n3 — family tokens are unreachable from n2 output), the emission rule and >20 stoplist semantics matching fixed decisions 4/5 exactly, and the volume arithmetic (17-34M band, ~19-22M point estimate, 1,260 windows, ≤20 IN-probes/window) against the measured 12,586,144/4,364,021 rows.


## MANDATORY AMENDMENTS — ops attacker findings

- DEFECT (Unit 4, walk termination — highest blast radius): the keyset window (SCAN_KEY_WINDOW=200_000 with 'drop the trailing possibly-partial key group, watermark = last COMPLETE key') has no defined behavior when ONE key's group holds >= the whole window. The page then contains a single, entirely-partial key group, there is no 'last complete key', the watermark cannot advance, and the loop re-reads the same page forever — a hang inside the serialized job queue, which blocks every daily probe/process/fold behind it (supervisor comment at /home/user/tender-db/crates/app/src/supervisor.rs:4561-4562: jobs run sequentially). Post-issue-234 an over-200k group is unlikely but the stoplist exists precisely because generic keys cluster ('Gymnazium' class), and the failure mode is an infinite loop, not a slow window. Fix in-plan: when a full page yields zero complete groups, stream that key to exhaustion (or count-first via SELECT COUNT(*) WHERE key_kind=? AND key=?), stoplist it, and advance the watermark past it.

- DEFECT (Unit 4 step 6, transaction/bind arithmetic): the edge-upsert read-back — 'read existing (org_a,org_b,rule) via IN probes on the PK' at IN_CHUNK=512 — is unimplementable as specified. A composite-PK probe needs 3 binds per tuple: 512 x 3 = 1,536 binds, over the 999 ceiling that IN_CHUNK exists to respect (/home/user/tender-db/crates/store/src/canonical.rs:8834-8836). Worse, row-value IN ('(a,b,rule) IN ((?,?,?),...)') appears NOWHERE in the codebase (grep confirms), so it violates the plan's own rule 'only constructs the codebase already exercises on turso'. Fix: probe by org_a alone in 512-id chunks (PK-prefix seek, filter (org_b,rule) in Rust) or chunk triples at <= 300 (MAX_BATCH_BIND 900 / 3 cols).

- DEFECT (Unit 3 finish, crash-resume dishonesty): finish_org_match_keys runs CREATE INDEX -> TRUNCATE -> ANALYZE -> reset watermark to 0 as separate steps. A crash after the index builds but before the watermark reset leaves watermark > 0; queue recovery re-runs the wet job, the resume path (watermark>0, no reset) walks zero rows and calls finish again — whose bare 'CREATE INDEX org_match_keys_kk' now FAILS on the existing index, wedging the job in a crash-rerun-fail loop. reset_org_match_keys uses DROP INDEX IF EXISTS; finish must symmetrically use CREATE INDEX IF NOT EXISTS (the codebase's own idiom, e.g. /home/user/tender-db/crates/store/src/canonical.rs:2506,2543) and treat ANALYZE/checkpoint as re-runnable.

- DEFECT (Unit 3, resume across a deploy = silent mixed-semantics table): the resume rule 'wet and watermark > 0 -> resume, do NOT reset' has no guard on WHICH key semantics wrote windows 1..k. A wet build stopped/crashed, followed by a deploy that changes n2/n3 (unit 6's n3s rollout is exactly this), resumes with the NEW fns from the old watermark: rows below the watermark carry old-semantics keys, rows above carry new, nothing detects it, and the scan then emits edges from a key space no single contract describes. Fix: persist a semantics epoch (a version int or a hash of a canary key-vector) beside the watermark in the same txn; on wet start with watermark > 0, mismatch => forced reset instead of resume. Related gap: there is NO operator path to force a fresh build while watermark > 0 (the run arm never resets then) short of hand-editing projection_state — give the Spec a fresh:bool.

- DEFECT (Unit 3/5, WAL mechanism misattributed; no WAL backpressure): the plan claims 'heavy_write registration is what buys the TRUNCATE instants'. False — heavy_write_in_progress only stands DOWN the dashboard coverage refresher (/home/user/tender-db/crates/app/src/coverage.rs:121) and the presence observer (supervisor.rs:4592); it does nothing about ordinary serving reads. The defense against overlapping short API reads denying the truncate its instant is the issue-63 WAL read-gate via Db::checkpoint_gated (/home/user/tender-db/crates/store/src/checkpoint.rs:93-118), which is opt-in and default-OFF (issue 63 board: 'busy=true -> set TENDER_WAL_READ_GATE=1') and which the projection loop uses (/home/user/tender-db/crates/ingest/src/project.rs:1359) — the plan never mentions it. A busy TRUNCATE returns promptly with partial reclaim (checkpoint.rs tests :299-306), so under a continuous read stream the build's 1,260 per-window truncates can all come back busy while the WAL grows for the job's whole life. The 307-backfill class survived on plain checkpoints, so this is probably survivable — but the plan must (a) correct the mechanism claim, (b) log wal_bytes() in the per-window journal line and define an abort/alert threshold (the build is resumable — stopping on a ballooning WAL costs nothing), and (c) name checkpoint_gated + the env flag as the escalation, per the issue-63 runbook.

- DEFECT (Unit 3, index sizing extrapolates a constant outside its measured domain): the 1.63 GB estimate uses INDEX_BUILD_BYTES_PER_ROW=48, measured ONLY on narrow keys — (country,id), (source,id) — and the constant's own doc says it must be RAISED the moment a wider key measures above it (/home/user/tender-db/crates/store/src/canonical.rs:36-59). org_match_keys_kk keys on a normalized NAME string (tens of bytes, sometimes hundreds) plus kind plus org_id — a categorically wider key than anything in the table at :43-46. Realistic peak is plausibly 3-5 GB; still far under AUTO_INDEX_MEMORY_BUDGET=12 GB, so the conclusion holds, but the plan should state the estimate is out-of-domain, record the first prod build's measured RSS in the journal, and raise the constant if it exceeds 48 B/row — that first build IS the wider-key measurement the comment demands. Same optimism in the table estimate: '~55 B/row' assumes short names; long official names push the table+index toward ~6-8 GB total. Harmless on disk, wrong as written.

- DEFECT (Unit 3, duration anchors are shakier than stated): (a) 'org-merge-health walks the corpus in ~2 min' — org_merge_health_batch filters WHERE identifier IS NOT NULL (/home/user/tender-db/crates/store/src/canonical.rs:6426-6428), i.e. it walks the ~1.12M canonical rows, NOT the 12.6M corpus; the build walk is ~11x that plus NFKC per name, so the read-phase base is off an order of magnitude. (b) The '307 backfill measured class (38.7M REPLACE ops)' carries NO recorded wall-clock anywhere in /home/user/tender-db/.scratch/tender-db/issues/307-org-names-satellite-backfill.md — 'measured class' is a volume analogy, not a duration measurement. (c) The codebase's own scar tissue warns about exactly this: REPORT_TICK's comment (supervisor.rs:4477-4481) records partial samples mispredicting whole-job duration by 4x, three times, in both directions. The 15-30 min / 45-90 min / <2 h numbers are estimates; the plan should demote them to guesses and make the DRY run's per-window timings the only number the rollout gates on (it already reads them in step 1 — make the gate explicit: no wet run until dry projects a wall-clock).

- DEFECT (Unit 4, scan duration entirely unestimated + a point-read storm design): the plan gives zero duration for the scan yet schedules it weekly. Its shape is a per-GROUP IN probe into organizations (step 3) plus per-PAIR re-reads via org_all_names — two point queries per org per pair (/home/user/tender-db/crates/store/src/canonical.rs:7217-7233) — over potentially 10^5-10^6 groups and pairs, and the wet run performs the census walk AND the emit pass (the R2 parity shape), roughly doubling it. Millions of pooled point reads is a different regime from the windowed scans every precedent cites; plausibly 1-3 h, unknown until run. Two fixes: (1) classify canonical-vs-provisional from a preloaded id set — the ~1.12M identifier-bearing ids load in seconds (the R2 preload the plan already name-drops) and fit trivially in RAM, eliminating the per-group probes entirely (live-row existence falls out of the same preload's id set or a per-window bulk IN over the window's member ids: ~400 chunked probes per 200k-row window instead of one per group); (2) record the census phase's wall-clock in the plan report and GATE step 6 (the Sunday tick flip) on it. Also note: org_all_names returns bare names WITHOUT lang labels, but step 4's e3-xlang rule needs 'the reaching rows' langs differ' — a new (source,lang,name) read fn is required, so the probe cost should be budgeted against that fn, not org_all_names.

- DEFECT (Unit 5, Sunday-chain budget never checked): the pre-dawn Sunday tick already carries data-quality — measured 39 min and the queue is serialized for all of it — plus rehash-probe (supervisor.rs:4463-4542), and its safety argument is 'finishes ~03:50, five hours clear of the 09:35 daily'. Appending an unmeasured multi-hour scan consumes that margin blind: an overrun past 09:35 makes the daily probe/process/fold queue behind the scan, sliding the TED finality-window re-fetch ('a daily may be rewritten until 09:30 CET', supervisor.rs:4624-4625) and the day's fold by however long the scan overruns. The plan must add the measured scan duration to the chain arithmetic before step 6, and state the acceptable worst case (or move the scan to its own slot, e.g. Saturday pre-dawn, where nothing else queues).

- DEFECT (Unit 5 rollout, wet build timing vs the daily window unstated): rollout steps 1-5 are operator-fired 'box serves reads throughout' with no instruction on WHEN. A wet build fired mid-morning holds the serialized queue for its whole (unmeasured, est. <2 h) life — the 09:35 daily chain queues behind it and the fold is late by the remainder. Since the queue structurally prevents overlap (heavy writes can never run DURING ingest/fold — they only delay it), the operating-rule question reduces to queue latency, and the plan should say so and direct: fire the wet build immediately after a completed daily fold or in the pre-dawn slot, never in the 09:00-12:00 band.

- DEFECT (Unit 5, the weekly wet scan will silently stop running — three separate refusal paths, no alarm on any): the T4 gate compares each wet run's live census against ONE stored dry plan ('org-edge-scan-plan', same keys build epoch) with tolerance max(2%, 500 edges). Nothing ever refreshes that plan: daily ingest keeps flipping provisionals to canonical (new identifiers), R2/R3 keep merging, so would-emit drifts monotonically away from the reviewed number and the Sunday wet run starts ABORTING every week once drift crosses the floor — likely within weeks at a 500-edge floor. Two more standing refusals: after any keys rebuild the plan's epoch mismatches (wet refuses until a human runs and reviews a new dry), and after a bare org rebuild has_index fails. All three land as a refused job at 03:xx Sunday that only a job-log reader ever sees, and tripwire 6's clock — which IS the weekly wet run — stops without anyone being told. Fix: (a) steady-state parity should compare against the LAST COMPLETED WET report's census (re-anchoring weekly) with the hand-reviewed dry reserved for first-run and post-rebuild acceptance, or schedule a dry+wet pair; (b) any refusal on the SCHEDULED path must eprintln loudly AND write a report/alarm field an operator surface shows — a silently stopped tripwire is the muted-probe failure the presence observer's own comment warns about (supervisor.rs:4573-4575).

- DEFECT (Unit 4, stop latency unbounded and unstated): the build checks cancelled() per 10k-org window (seconds — genuinely stoppable, consistent with STOPPABLE_KINDS' honesty contract at supervisor.rs:1163-1185). The scan checks stop() 'between windows', but a scan window = 200k index rows PLUS all of its group-classification probes and pair labeling — under the current per-group/per-pair probe design plausibly minutes per window. The issue-252 lesson behind STOPPABLE_KINDS was precisely a 'cancelled' job that ground on for six minutes. State the worst-case stop latency per window, or add a stop check between classification chunks inside the window.

- CORRECTION (Unit 3 sizing note, disk framing is stale, conclusion still holds): 'steady ~2.5-4 GB on the 1.7 TB box' cites total disk, but issue 169 (/home/user/tender-db/.scratch/tender-db/issues/169-storage-lifecycle-model-at-half-a-terabyte.md) shows the real ledger: /data was ~632 G free on 2026-08-28, a fresh 523.7 GB reflink snapshot is held, two superseded snapshots (~950 G reclaimable) await a classifier-permitted rm, and the model event of 08-28 is that whole-corpus write campaigns diverge every held reflink snapshot toward a full copy — snapshot COW divergence, not table size, is what consumes disk here. Also: turso VACUUM OOMs ('a DB can never shrink'), so the first build's +~4-8 GB is permanent file growth and every DROP+CREATE rebuild only recycles freelist pages. A ~4-8 GB satellite plus bounded WAL is genuinely safe against ~600 G free — but the plan should quote the issue-169 numbers, note the snapshot-divergence trickle its writes add, and budget against post-cleanup free space like issue 304 was told to.

- SOUND, VERIFIED (for the record, so review effort goes to the defects above): (a) watermark-committed-in-the-same-txn per window is the right exactly-once shape and makes crash-resume free; (b) index-free bulk load with ONE post-load CREATE INDEX is the correct strategy per issues 60/62 (random-position probe storms) and 205 (schema-batch builds hanging boot — canonical.rs:339-357, 2473-2546); building it inside the job respects the sequential-builds constraint; (c) drop_and_recreate for reset is right and specifically avoids the DELETE-FROM per-row-WAL-frame OOM (canonical.rs:2848-2875 — O(1) WAL); (d) 10k-org windows / ~17k-row write txns sit inside the NODE_WRITE_BATCH=20_000 band (canonical.rs:849) and flush_rows' 900-bind chunking handles 3-col rows at 300/statement (canonical.rs:8741-8779); (e) MIGRATIONS additions match the rederive_eur_watermark add_column precedent exactly (/home/user/tender-db/crates/store/src/lib.rs:622-628); (f) heavy_write_kind listing for both jobs is right under the issue-281 over-inclusion rule, and no-auto-enqueue-at-boot is consistent with queue recovery; (g) the sequential job queue structurally keeps these writers out of ingest/fold windows — the exposure is delay, not overlap, which is why the two scheduling defects above are about latency budgets rather than corruption.

